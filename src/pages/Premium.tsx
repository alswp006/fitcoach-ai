// @AI:NOTE TDD red phase stub — intentionally unimplemented so packet-0019 tests fail (RED).
// Coder packet implements the real S8 프리미엄('/premium') 화면 here.
export default function Premium() {
  return null;
}
