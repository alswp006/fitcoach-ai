// @AI:NOTE TDD red phase stub — intentionally unimplemented so packet-0018 tests fail (RED).
// Coder packet implements the real S7 기록('/history') 화면 here.
export default function History() {
  return null;
}
