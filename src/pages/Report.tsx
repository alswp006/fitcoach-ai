// @AI:NOTE TDD red phase stub — intentionally unimplemented so packet-0017 tests fail (RED).
// Coder packet implements the real S6 AI 리포트 화면 here.
export default function Report() {
  return null;
}
