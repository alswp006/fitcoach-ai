/**
 * 로컬 더미 생성 로직 — VITE_AI_API_BASE_URL 미설정 시 폴백
 * 텍스트는 한국어 해체체 고정, 리포트는 aiGenerated 메타 필드 포함
 */

export function generateLocalPlanSummary(input: string): string {
  // Stub — tests will fail until proper implementation is added
  return "";
}

export function generateLocalFeedback(input: string): string {
  // Stub — tests will fail until proper implementation is added
  return "";
}

export function generateLocalReport(input: any): any {
  // Stub — tests will fail until proper implementation is added
  return {};
}
